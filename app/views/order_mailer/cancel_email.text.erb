Dear Customer,

Some items from your order have been CANCELLED.  Please retain this cancellation information for your records.

============================================================
Order Summary [All CANCELLED Items]
============================================================
<% for unit in @order.inventory_units.canceled %>
<%= unit.line_item.variant.product.name if unit.line_item.variant and unit.line_item.variant.product %> <%= variant_options(unit.line_item.variant) if unit.line_item.variant %> <%= number_to_currency unit.line_item.price %> 
<% end %>
============================================================

============================================================
New Order Amount
============================================================
Order Subtotal: <%= number_to_currency @order.item_total %>
<% @order.adjustments.each do |adjustment| %>
<%= "#{adjustment.label}: #{number_to_currency adjustment.amount}"%>
<% end %>
Order Total: <%= number_to_currency @order.total %>
