<% if !user_product.nil? %>
	<%= link_to user_product.user.display_name, entity_profile_path(user_product.user) %> added <%= link_to user_product.name, uploaded_member_collection_path(user_product.user) %> to their collection.
<% end %>